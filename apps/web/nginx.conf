server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    location = /favicon.ico {
        return 302 /vite.svg;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}